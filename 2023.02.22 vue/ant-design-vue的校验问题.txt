问题背景：
1：页面中有多个rules的校验，并且有多个校验是用v-if控制的；
2：有的校验成功，有的校验失败；
问题原因：
1：渲染方式的原因，v-if指令会根据表达式的真假有条件地渲染元素，当表达式为真时，元素将被渲染，元素将不会被渲染，
2：v-if具有“惰性”特性，即只有当条件第一次满足时，元素才会被创建和插入DOM；
3：v-show指令也会根据表达式的真假有条件地渲染元素，无论表达式为真或假，元素始终会被创建并被插入DOM等。
解决方式：
将页面中的所有v-if用v-show替代。